#include "../../include//pieceList.h"

pieceList generatePieceList(chessBoard *board) {
  for (int i = 0; i < NUMBEROFCOLORS; i++) {
    for (int j = 0; j < NUMBEROFDIFFERENTPIECES; j++) {
    }
  }
}
